.container
  br
  .col-md-4
    h3 Disponibilidad en mi #{@calendar.name}
    #booking-form
    #booking-items
      =link_to "Agrega Disponibilidad", new_calendar_booking_path(@calendar), remote: true, class: 'btn btn-success pull-right'
      table#booking-table.bookings_table.table.table-striped
        thead
          tr
            th Servicio
            th Inicio
            th Termino
            th Estado
            th
        tbody
          - if @bookings.count > 0
            = render 'bookings'
          - else
            #booking-void.jumbotron
              h3 Agrega tu disponibilidad
              p Agrega tus horarios disponibles! Para los paseos agrega tu disponibilidad por hora y para guardería y alojamiento agrega la disponibilidad de todas las horas que vas a cuidar de un perro.
              =link_to 'Agregar', new_calendar_booking_path(@calendar), remote: true, class: 'btn btn-sm btn-success'

  .col-md-8
    .calendar_container
      #calendar
